spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/clinicaspringboot
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
